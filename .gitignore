# Node
node_modules/
.next/
# Butun .env varyantlari (.env.vercel, .env.production vb. dahil); yalniz
# ornek dosya izlenir. Tek tek desen yazmak .env.vercel'i disarida birakmisti.
.env*
!.env.example

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Build
dist/
build/
out/
tsconfig.tsbuildinfo

.vercel

# Supabase CLI'nin yerel baglanti/metaveri artefaktlari
/supabase/.temp/

# Soru bankasi veri dosyalari (full dataset DB'de, repo'da olmamali)
data/soru-bankasi/
wordquest/data/*
# Sample dosyasi istisna — dev seed testi icin gerekli (~20 soru)
!wordquest/data/questions-sample.json
# Private klasor — full dataset ve diger gizli assetler
/private/

# PWA: next-pwa kaldirildi (Next 16 uyumsuzdu, sw.js uretmiyordu). Manuel
# public/sw.js committed (build_id-bagimsiz vanilla SW API). next-pwa
# artifact'leri (workbox-*, swe-worker-*, fallback-*) silindi (kullanilmiyor).

# Playwright e2e artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Vitest coverage (lcov + html, codecov upload icin lokal artifact)
/coverage/

# VPS infra secrets (DB passwords, JWT secrets, env files - VPS-only)
infra/vps/*/secrets/*.env
infra/vps/*/secrets/*.json
!infra/vps/*/secrets/.gitkeep

# LLM judge audit run artifacts (lokal calisma ciktilari, fixture bunlardan ayri)
database/audit-claude-batch.json
database/audit-claude-stats.json
database/audit-llm-judge-report.json
database/audit-semantic-report.json
database/audit-validate-report.json
database/__tests__/_eval-input.json
database/__tests__/_eval-output.json
database/__tests__/eval-judge-report.json
database/import-judge-audit-*.json
database/audit-math-fen-result.json
database/generated/*
!database/generated/lgs-*.json
!database/generated/ayt-*.json
database/backup-*.json
database/_*.json
database/_*.log
# Otomatik soru dogrulama kosu ciktilari (question-audit hatti).
# Bulgu kanitlari soru kokunu ve TUM sik metinlerini birebir tasir; soru
# bankasi bilerek git gecmisinden temizlenmisti, geri sizmamali.
# Kosucunun varsayilan ciktisi da bu desene girer (runner.ts `out`).
database/question-audit-*.json
# İnsan uzman etiketleri ve held-out gold set repo dışında tutulur.
/secure/
.env*.local

# Lokal denetim/dogrulama artiklari (ekran goruntuleri, handoff, audit run ciktilari)
/_*.png
/_*.mjs
/HANDOFF-*.md
/qprev.json
database/OUTPUT_REVIEW.json
database/apply-review-manual-queue.json
database/audit-active-*.json
database/audit-active-*.csv
database/audit-keybug-*.json
database/audit-report-*.json
database/zero-acc-*.json
database/dedup-backup-*.json

# Ham ÖSYM sınav-artifactleri (TELİFLİ — repo-dışı; few-shot yalnız gitignore'lu _ref-2026/ özet kullanır)
tyt_2024.pdf
tyt_text.txt
extracted_qs.txt
*_full.txt
*_key.txt
